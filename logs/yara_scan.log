import logging

logging.basicConfig(filename="logs/yara_scan.log",
                    level=logging.INFO,
                    format="%(asctime)s - %(levelname)s - %(message)s")

logging.info("YARA scan executed.")
